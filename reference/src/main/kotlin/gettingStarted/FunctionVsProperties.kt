package gettingStarted

// Functions with no args might be interchangeable with read only properties, similar in semantic, stylistic convention
// are present.

// Property when the underlying algorithm:
// no exceptions
// has O(1) complexity
// cheap to calculate (or cache on first run)
// returns the same result over invocations(idempotent)